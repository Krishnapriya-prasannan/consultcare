import React from 'react';
import DocLeave from '../../DoctorPage/DocComponents/docLeave';
const AdLeave =({ isAdmin }) => {
  return (
      <DocLeave isAdmin={isAdmin} />
  );
};

export default AdLeave;
